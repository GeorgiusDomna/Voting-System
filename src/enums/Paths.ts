export enum Paths {
  ROOT = '/',
  LOGIN = '/login',
  REGISTRATION = '/registration',
  DOCUMENTS = '/documents',
  USER_DOCUMENTS = '/user-documents',
  DOCUMENTS_VOTE = '/documents-vote',
  ANY = '*',
}
